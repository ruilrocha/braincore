# brain-io dependencies managed by Conan.

requirements:
  # FFT (PocketFFT — header-only)
  - "pocketfft/0.0.0.cci.20240801"

  # Unit tests
  - "gtest/1.14.0"

  # Micro-benchmarks (opt-in: cmake … -DENABLE_BENCHMARKS=ON)
  - "benchmark/1.8.3"
